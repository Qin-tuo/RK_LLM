# RKNN3 Toolkit vendor wheels come from its complete upstream checkout, not PyPI.
# The repository and revision are pinned in manifests/upstream.yaml; the host
# bootstrap workflow verifies that checkout identity before it can be used.
